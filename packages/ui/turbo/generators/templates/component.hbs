import { ReactNode } from "react";

interface {{ pascalCase name }}Props {
  children: ReactNode;
}

export default function {{ pascalCase name }}({ children }: Readonly<{{ pascalCase name }}Props>): JSX.Element {
  return ();
};
